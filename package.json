{
  "name": "sqlite-metadata-proposal",
  "description": "Workspace for the SQLite Metadata Platform spec & supporting packages",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "pnpm -r format",
    "check": "pnpm run lint && pnpm run test"
  }
}
